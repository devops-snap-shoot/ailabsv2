declare const host: string;
declare const database: string;
declare const port: number;
declare const completUrl: string;
declare const dbConfig: {
    host: string;
    database: string;
    port: number;
};
export { host, database, port, completUrl };
export default dbConfig;
